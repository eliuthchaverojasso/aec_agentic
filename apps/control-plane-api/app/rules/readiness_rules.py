"""Readiness rule-pack placeholder.

The active readiness rules currently live in ``app.readiness.rules`` because
they operate on requirements, compliance rows, issues, and latest sync state
rather than a single Revit element. This module marks the future home for the
same rules once the full cross-domain RuleContext is adopted.
"""
