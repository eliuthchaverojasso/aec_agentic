[project]
name = "control-plane-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
aec = "control_plane_cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

