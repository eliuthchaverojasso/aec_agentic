# Runtime State

This directory is for local runtime state pointers, non-secret checkpoints, packet templates, and operational notes.

Do not store secrets, raw client data, model caches, database dumps, or large generated artifacts here.

