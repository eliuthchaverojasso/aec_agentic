\section{Revit Add-in Workflow}
The add-in supports project binding, landing configuration, landing validation, JSON export, and metadata sidecars. Export metadata may indicate backend ingestion status as not submitted until web operators execute Processing/Sync ingest.

