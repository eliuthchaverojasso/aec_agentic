\section{System Architecture}
EMA AI connects Revit Add-in exports to a local landing zone, processes them through FastAPI services, persists official state in PostgreSQL, and presents status through a React dashboard. Debug diagnostics provide operational transparency without changing official readiness values.

