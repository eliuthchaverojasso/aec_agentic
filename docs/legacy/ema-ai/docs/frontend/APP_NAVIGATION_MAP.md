# App Navigation Map

## Global

- Login / Welcome (`/login`)
- Executive Overview (`projects`)
- Projects Portfolio (`projects`)
- Enterprise Issues (`issues`)
- Compliance (`compliance`)
- Reports (`reports`)

## Project

- Project Setup (`projectSetup`)
- Deliverable Tracker (`overview`)
- Model / Viewer (`viewer`)
- Trade Readiness (`trades`)
- Requirements (`requirements`)
- Drawing Reel (`drawingReel`)
- Model Health (`modelHealth`)
- Documents / Evidence (`documents`)
- Processing / Sync (`processing`)

## Admin

- Settings (`settings`)
- Users (`users`)
- Roles & Permissions (`roles`)
- Appearance (`appearance`)
- Dev Mode (`devMode`)
- System Health (`systemHealth`)
- Debug / Logs (`debugLogs`)

Every route renders a page and should show either backend data, a safe operation surface, or a clear empty/planned state.
