# Release Checklist (Local Workflow Pass)

- Backend compile passes.
- Backend tests pass.
- Frontend build passes.
- Project Setup wizard can create or bootstrap project.
- Selected project persists via `ema-ai-selected-project-id`.
- Processing uses project-scoped landing routes (no ingest route mismatch 404).
- Real project artifacts are not staged.
- `opencode.json` remains unstaged.
- Existing PR updated (no duplicate PR).
