AEC_ENV=local
DATABASE_URL=postgresql+psycopg://ema:ema_dev_pw@localhost:5432/ema_ai
API_PORT=8010
WEB_PORT=5173
OBJECT_STORE_ENDPOINT=
OBJECT_STORE_BUCKET=aec-control-plane-local

# Web console (Vite). Optional: prefill the project-setup "landing root" field.
# Leave unset to ship no machine-specific path (the field stays user-editable).
# VITE_DEFAULT_LANDING_ROOT=

